import { FOOTER } from "../copy";

export function Footer() {
  return (
    <footer className="site-footer">
      <div className="desk-rail">
        <p>{FOOTER}</p>
      </div>
    </footer>
  );
}
